name: Release

on:
  push:
    branches:
      - main

jobs:
  release:
    uses: heliomarpm/reusable-actions/.github/workflows/release.yml@main
    secrets:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
